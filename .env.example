# terminal.sh Configuration
# Copy this file to .env and modify as needed
# .env files are gitignored and used for local development

# Server Configuration
HOST=0.0.0.0
PORT=2222
WEB_HOST=0.0.0.0
WEB_PORT=8080
# SSH Host Key Path (optional)
# If empty, defaults to .ssh/ssh_host_key
# The Wish framework will auto-generate the key if it doesn't exist
# Keep this consistent across restarts to avoid "host key changed" warnings
HOSTKEY_PATH=

# Database Configuration
# For SQLite (default):
DATABASE_PATH=data/terminal.db

# For PostgreSQL (uncomment and set):
# DATABASE_URL=postgres://user:password@localhost:5432/terminal_sh

# Security
JWT_SECRET=change-this-secret-key-in-production
